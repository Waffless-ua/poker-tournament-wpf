<UserControl x:Class="TexasHoldemWPF.Controls.BalanceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="250">

    <Grid>
        <Border Background="#FF2D2D30" CornerRadius="20" Padding="10,0,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0"
                       Source="pack://application:,,,/Resources/Icons/Chip.png"
                       Width="30" 
                       Height="30"    
                       HorizontalAlignment="Left" Margin="-10,0,0,0"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding Balance, StringFormat=${0}, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Foreground="White"
                           FontSize="24"
                           Margin="10 0 10 3"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="2"
                    Command="{Binding AddMoneyCommand}"
                    Width="30" 
                    Height="30"
                    BorderThickness="0"
                    BorderBrush="Transparent"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Padding="0">    
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Ellipse Fill="{TemplateBinding Background}"/>
                                <Image Source="pack://application:,,,/Resources/Icons/Plus.png"
                       Width="35"
                       Height="55"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>


            </Grid>
        </Border>
    </Grid>
</UserControl>